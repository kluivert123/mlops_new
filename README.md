# mlops_new
# To install flask
1. Create a virtual environment with py -3 -m venv venv
2. activate the environment with venv\Scripts\activate
3. within the activated environment, use pip install Flask to install the app.
4. you can run the app in your powershell with flask --app app run